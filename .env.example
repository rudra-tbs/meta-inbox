# WhatsApp / Meta
WHATSAPP_ACCESS_TOKEN=        # Permanent system user token from Meta Business
WHATSAPP_APP_SECRET=          # App Secret from Meta App Dashboard → Settings → Basic
WHATSAPP_PHONE_NUMBER_ID=     # Phone number ID from Meta app API setup
WHATSAPP_VERIFY_TOKEN=        # Any string you choose; used to verify webhook

# Instagram / Meta
INSTAGRAM_PAGE_ID=            # Facebook Page ID connected to your Instagram Business account
INSTAGRAM_ACCESS_TOKEN=       # Page access token (same app, page must have instagram_manage_messages permission)

# OpenRouter
OPENROUTER_API_KEY=           # From openrouter.ai → Settings → API Keys
OPENROUTER_MODEL=             # e.g. minimax/minimax-01 or anthropic/claude-3-haiku

# Supabase
NEXT_PUBLIC_SUPABASE_URL=     # Project URL from Supabase → Connect → API
NEXT_PUBLIC_SUPABASE_ANON_KEY= # Anon key from Supabase → Settings → API
SUPABASE_SERVICE_ROLE_KEY=    # Service role key (server-side only, never expose to client)

# CRM MySQL (thebrideside)
CRM_MYSQL_HOST=               # MySQL host / IP
CRM_MYSQL_PORT=3306           # Default 3306
CRM_MYSQL_USER=               # MySQL username
CRM_MYSQL_PASSWORD=           # MySQL password
CRM_MYSQL_DATABASE=thebrideside

# App
NEXT_PUBLIC_APP_URL=          # e.g. https://whatsapp.acceltancy.in or localhost:3000
